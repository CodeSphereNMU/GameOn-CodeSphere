# Diagrams

Store system diagrams here, including:

- Use case diagrams (updated from FSSB)
- Domain class diagrams
- Database ERD (from SQL Server)
- Sequence diagrams for complex flows (e.g., join request lifecycle)
- Architecture overview diagram

Export diagrams as `.png` or `.svg` for easy viewing. Keep editable source files (Visio, draw.io, etc.) alongside them if practical.
