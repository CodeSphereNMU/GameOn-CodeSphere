-- GameOn Database Baseline
-- This migration establishes the schema versioning baseline.
-- Feature-specific tables will be added in subsequent migrations
-- as each feature spec is approved and implemented.
--
-- Naming convention for migration files:
--   V<number>__<short_description>.sql
--   Example: V2__create_users_table.sql
--
-- Rules:
--   - Never modify a migration after it has been applied.
--   - Each migration must be idempotent where practical.
--   - Use parameterised SQL in application code; raw DDL here is fine.

-- Placeholder: confirms Flyway is working against this database.
PRINT 'GameOn baseline migration applied successfully.';
