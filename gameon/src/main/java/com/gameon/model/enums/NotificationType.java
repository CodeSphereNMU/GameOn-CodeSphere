package com.gameon.model.enums;

public enum NotificationType {
    FOLLOW_NEW,
    JOIN_REQUEST_RECEIVED,
    JOIN_ACCEPTED,
    JOIN_REJECTED,
    GAME_REMINDER,
    MATCH_RESULT_POSTED,
    LISTING_CANCELLED,
    LISTING_INVITE
}
